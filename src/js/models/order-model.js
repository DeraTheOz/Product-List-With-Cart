import { cartModel } from '../models/cart-model.js';
import { resolveImagePaths } from '../utils/resolve-image-paths.js';

// Generates an Order object that includes items, prices, images
// Validate order data
export const orderModel = {};
